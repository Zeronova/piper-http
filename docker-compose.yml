services:
  piper:
    build: .
    container_name: piper
    ports:
      - "${PIPER_PORT:-5000}:5000"
    env_file: piper.env
    volumes:
      - "${PIPER_MODELS_DIR:-./models}:/app/models"
    restart: unless-stopped
